import { Routes } from '@angular/router';
import { RenderDateComponent } from './render-date/render-date.component';
export const routes: Routes = [
    {path: 'renderdate', component: RenderDateComponent}
];
